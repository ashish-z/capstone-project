# --- Core LLM + Agent framework ---
langchain>=0.3.0,<0.4.0
langchain-anthropic>=0.3.0,<0.4.0
langchain-community>=0.3.0,<0.4.0
langgraph>=0.2.40,<0.3.0
langsmith>=0.1.130

# --- Retrieval / RAG ---
chromadb>=0.5.20,<0.7.0
sentence-transformers>=3.0.0,<4.0.0

# --- LLM SDK (used by langchain-anthropic) ---
anthropic>=0.40.0

# --- App / UI ---
streamlit>=1.40.0,<2.0.0

# --- Data + utilities ---
# NumPy pinned to 1.x — torch 2.2.2 is the last x86_64 macOS build and was
# compiled against NumPy 1.x. Using NumPy 2.x triggers ABI incompat at import.
numpy>=1.26,<2.0
pydantic>=2.10.0,<3.0.0
python-dotenv>=1.0.0
pyyaml>=6.0
httpx>=0.27.0
loguru>=0.7.0
tenacity>=9.0.0

# --- Evaluation ---
ragas>=0.2.0,<0.3.0
datasets>=3.0.0

# --- Dev / Test ---
pytest>=8.0.0
pytest-asyncio>=0.24.0
ruff>=0.8.0
